---
import "../styles/form.css"

interface Props {
  id: string;
}

const { id } = Astro.props
---

<script is:inline define:vars={{id}}>
  hbspt.forms.create({
    region: "na1",
    portalId: "6604339",
    formId: id
  });
</script>
